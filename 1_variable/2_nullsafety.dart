// Variables of higher level don't need to be initialized after 
//Don't is permitted
// String completeNameHigher;
void main() {

  // Local variables don't need to be initialized at face value but
  // can be created without a value and assigned a value later.
  String completeName;
  var a;
  var b;

  completeName = 'Charles David Winkler';
  a = 1;
  b = 1.2;
  print(completeName.length);
}
